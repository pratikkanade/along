# Copy to .env.local. Optional — the app defaults to http://localhost:4000.
# Point this at the Rally backend (Fastify). Must be reachable from the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
